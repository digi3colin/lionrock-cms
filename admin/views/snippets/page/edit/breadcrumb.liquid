<div class="container">
    <div class="row">
        <div class="col-8">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/admin">Home</a></li>
                    {% for block in section.blocks %}
                        <li class="breadcrumb-item"><a href="">{{ label }}</a></li>
                    {% endfor %}

                    <li class="breadcrumb-item active" aria-current="page">{{ item.name | default: item.id }}</li>
                </ol>
            </nav>
        </div>

        <div class="col-4">
            <a class="label-icon toolset-add float-end" href="/admin/contents/create/{{ slug }}">New {{ slug }}</a>
        </div>
    </div>
</div>