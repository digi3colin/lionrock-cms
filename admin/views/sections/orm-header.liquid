<div class="container mx-auto px-4">
  <header>
    <div class="md:flex md:items-center md:justify-between">
      <div class="min-w-0 flex-1">
        <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:truncate sm:text-3xl sm:tracking-tight">{{ type.tableName | capitalize }}</h2>
      </div>
      <div class="mt-4 flex md:ml-4 md:mt-0">
        <a class="button-secondary" href="/admin/{{type.tableName}}/new">Create {{type.name}}</a>
      </div>
    </div>
  </header>
</div>